rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Helper functions
    function isAdmin() {
      return request.auth != null && request.auth.token.role == 'admin';
    }
    
    function isValidImage() {
      return request.resource.size < 5 * 1024 * 1024 // 5MB max
        && request.resource.contentType.matches('image/.*'); // Only images
    }

    // Allow public read access to all files
    match /{allPaths=**} {
      allow read: if true;
    }

    // Facilities folder rules
    match /facilities/{facilityId}/{fileName} {
      allow write: if request.auth != null 
        && isValidImage()
        && (
          request.auth.uid == facilityId || // Owner
          isAdmin() // Admin
        );
    }

    // Licenses folder rules - Admin only
    match /licenses/{fileName} {
      allow write: if isAdmin() && isValidImage();
    }

    // Insurances folder rules - Admin only
    match /insurances/{fileName} {
      allow write: if isAdmin() && isValidImage();
    }

    // Locations folder rules - Admin only
    match /locations/{fileName} {
      allow write: if isAdmin() && isValidImage();
    }

    // Allow admins to delete files they have access to
    match /{path=**} {
      allow delete: if isAdmin();
    }
  }
}
